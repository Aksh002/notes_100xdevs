export default function fxn({children}:{children:React.ReactNode}){
    return <div>
        WELCOME MERE USER (from app/signin/user/layout.tsx)
        <div>
            {children}
        </div>
    </div>
}