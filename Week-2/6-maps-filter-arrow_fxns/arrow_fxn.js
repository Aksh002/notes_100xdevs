function sum(a,b){
    return a+b;
}
const ans=sum(1,2);
console.log(ans)

// Arrow fxns
const sum=(a,b)=>{
    return a+b;
}
// Both are ALMOST SAME, only differ when we use fxns for callbacks,there meaning can change time to time

//Similarly we can wrrite .get fxn like
app.get("/",(req,res)=>{
    
}) 